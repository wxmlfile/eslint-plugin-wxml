### wxml config suite

For exmaple

```js
module.exports = {
  extends: [
    // add more generic rulesets here, such as:
    // 'eslint:recommended',
    "plugin:wxml/wxml-recommended",
  ],
  rules: {
    // override rules here
  },
};
```
